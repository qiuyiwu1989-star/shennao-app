# 013 · 安卓版 V5 改进计划（2026-09-12，基于两份审计）

> 依据：`docs/audits/2026-09-12-android/` 两份审计（代码审计-第二份 + 另一会话的审计与开发建议），以及 PLAN.md 上还挂着的未决事项。
> 原则不变：先修最危险的边界，再重整体验，不重写、不换栈、不一次升级所有依赖。每一轮都能验、能发。

## 现状一句话

4.6.3 (61) 已发。链路「录 → 传 → 出判断 → 落承诺 → 被提醒」在 JVM 上全绿（402 单测），**在真机上一次都没完整跑过**。剩下的问题分四类：可靠性边界、会话隔离、采集与记录的体验、以及只有邱能做的事。

## 五轮

### 第 1 轮 · 可靠性收尾（1–2 天，纯客户端，可发 4.6.4）——✅ 4.6.4 (62)，1.7 未动

审计已修七处之后还剩的边界：

| # | 事 | 位置 | 验收 |
| --- | --- | --- | --- |
| 1.1 | drain 从通知泵里拆出去：泵只管每秒报时，上传在独立协程 | RecordingService.startPump | 传一段 2 分钟的文件，通知栏计时不停 |
| 1.2 | 续期锁不包网络：读 token 不加锁，只有「发续期请求」互斥 | Client.validAccessToken / refresh | 断网时点「今天」下拉，界面不卡 30 秒 |
| 1.3 | `sameOrigin` 端口归一（-1 与 443 等价） | MeetingWebScreen | SameOriginTest 加两条 |
| 1.4 | `FileVault.rename` 失败要落日志并报到 lastError | record/FileVault.kt, Uploader | 单测：rename 返回 false 时 DrainResult.Failed |
| 1.5 | Installer 装完清 `cache/apk/` 旧版本 | Installer | 装两版后目录只剩当前 |
| 1.6 | 失败记录的恢复入口（对方 A5）：没有 transcriptId 的记录也能打开详情；「不到 5 分钟默认不分析」不再染红当失败；每种状态都有对应动作（重试 / 手动分析 / 看原因 / 删除） | HistoryScreen, MaterialCard | 夹具 s4 那条不再显示「没成功」；每条记录都点得开 |
| 1.7 | 截图门禁的债：要么找到 IO 协程回主线程丢失的根因，要么把依赖取数的截图改成传入已加载数据的纯组件截图（不再在测试里跑 LaunchedEffect） | ui/ScreenshotTest, EdgeShotTest | verify 连跑 5 次全绿 |

### 第 2 轮 · 会话与账号边界（2–3 天，可发 4.7.0）——✅ 4.7.0 (63)，2.4 要服务端

两份审计都点到、还没动的：

| # | 事 | 决定点 |
| --- | --- | --- |
| 2.1 | 本地录音目录按 **userId** 隔离（现在只钉了 orgId）。换账号后上一个账号没传完的录音：显示但标明「属于 a@b.c，登录回去才会传」，不删 | 要邱定：显示 or 隐藏 |
| 2.2 | 退出登录时正在录音怎么办：默认「先停录并保留本地」，弹一句说明 | 文案 |
| 2.3 | 切组织期间迟到的请求不能覆盖新状态（今天页 / 记录页缓存按 orgId 分桶） | — |
| 2.4 | 网页版入场券从 URL 参数改成 POST 表单（服务端配合，可延后） | 服务端 |
| 2.5 | 定时聆听的边界测试：通知被拒、改时区、重启后、错过提醒 | ListenScheduleTest 加 4 条 |

### 第 3 轮 · 采集与记录体验（1 周，可发 4.8.0）——✅ 4.8.0 (64)；3.5 字体 ✅ 4.10.0

对方审计的「第一轮 / 第二轮」产品建议，与邱 09-11 的四条反馈合并：

| # | 事 | 说明 |
| --- | --- | --- |
| 3.1 | **一个采集控制面**：录音台合并「本次录音 / 持续聆听」两种模式，显示在听 / 实际录了多久 / 本地待传数 / 传到哪一站；定时与后台权限从这里可达 | 现在散在录音台 + 我的页四行 |
| 3.2 | **记录页默认时间线**：单列，标题 / 时间 / 时长 / 来源 / 状态 / 一两行摘要；双栏内容卡作为可切换视图；异常收成一条汇总入口，不再霸占首屏 | 邱说「参考妙记」——妙记也是时间线 |
| 3.3 | **「录完放到日历里」**：记录页顶部一条周带（7 天，每天一个点表示有没有录），点一天过滤；不是替换列表 | 需邱确认这是不是他要的「日历」 |
| 3.4 | 今天页首屏加「最近一次录音去哪了」一行（本地 → 传 → 转写 → 分析，当前站高亮） | 对方审计第三轮 |
| 3.5 | 拉丁字体：Outfit + Plus Jakarta Sans（各约 150 KB），和网页对齐 | **需邱说「可以」才下载** |
| 3.6 | 文案：关键状态用直白词（待跟进 / 上传失败 / 录音笔），品牌词并列保留 | CopyGuardTest 词表同步改 |

### 第 4 轮 · 只有邱能做的（穿插，越早越好）

| # | 事 | 为什么非他不可 |
| --- | --- | --- |
| 4.1 | **真机 + 灵魂卡实测**：锁屏续录、来电打断、断网恢复、蓝牙断连重连、低存储、重启、换账号、覆盖升级保留数据 | Robolectric 碰不到麦克风 / 蓝牙 / 前台服务 |
| 4.2 | 4.6.3 两条专项：全时聆听传完后仍「在听」；换账号后网页版不串 | 本轮修的两处 |
| 4.3 | 一个测试账号 | 登录后的屏我看不到 |
| 4.4 | 部署服务端分支 `agent/mobile-bff-phase2`（含两份迁移） | CLAUDE.md 红线：未经确认不部署不跑迁移 |
| 4.5 | 定主仓库 `clients/android` 冻结还是删除 | 两份安卓在分叉 |
| 4.6 | Android 10/11 真机验蓝牙（这次补了旧权限，没机器验） | — |

### 第 5 轮 · 工程卫生（有空就做，不阻塞发布）——4.8.1 (65) Lint 与 R8 规则；4.9.0 工具链；4.10.0 targetSdk 35 + Compose 1.8；4.10.1 开 R8（夹具冒烟过，真机回归待邱）

- targetSdk 34 → 35（前台服务类型、Edge-to-edge 强制，要过一遍每个 FGS 与 insets）
- R8：开 minify 前给 org.json / OkHttp / WorkManager / security-crypto 补 keep 规则，体积能砍一半
- Lint 33 警告清零（多数是依赖版本）
- CI 上加 `verifyRoborazziDebug`（等 1.7 稳了再加）

## 顺序与门禁

```
第 1 轮（4.6.4） → 第 4 轮 4.1/4.2 真机验 → 第 2 轮（4.7.0） → 第 3 轮（4.8.0） → 第 5 轮
```

每轮的门：402+ 单测绿、lint 0 error、`scripts/publish-android.sh` 一条到首页、PLAN.md 进度日志一行。
第 3 轮动 UI 前先出截图（夹具模式）给邱看一眼再铺开。

## 现在就要邱拍板的三件

1. 换账号后，上一个账号没传完的本地录音：**显示但标明**，还是**隐藏**？（2.1）
2. 「录完放到日历」是**周带过滤**（3.3）还是别的？
3. 拉丁字体下载：**可以**吗？（3.5）
