# 交付计划：一个完善可用的安卓版本

> 2026-09-05 邱完全授权：全面审计、自主执行、可以推翻此前决定，
> 只在**必须他配合**的事上找他。本文是执行的真源——跨会话接手先读这里。

## 「完成」的定义

不是「原型里的 32 屏都做完」，是下面这条链在真机上跑通，且每一环都能说清自己在哪一站：

```
拆盒 → 连上灵魂卡 → 录一场（卡或手机）→ 传到深脑 → 出判断 → 落一条承诺 → 第二天被提醒
```

外加三条硬约束：
- **三态齐全**：每一屏的 空 / 离线 / 失败 各自有话说，绝不画成同一屏
- **一屏一个决定**：首屏正文 ≤ 5 条，猜想默认折叠
- **不丢东西**：台账能回答「我的录音卡在哪一站」，且写明它看不到什么

## 审计结论（2026-09-05）

已有的比预想多，缺的比预想少：

| 层 | 状态 | 依据 |
| --- | --- | --- |
| BLE 协议 / 导入 / 幂等 | **完成，不动** | `ble/` 13 文件，真机踩坑记录齐全，26 条单测 |
| 手机录音 / 分片 / 上传 | 完成，后台存活未验 | `record/` 13 文件 |
| 导航 | **完成**（2026-09-05 重构） | `Nav.kt`，单一真源，12 条门禁测试 |
| 链路四站 | **已画好** | `HistoryScreen.Chain(stage)` |
| 今天 · 四段 · 三档认知 | 完成 | 2026-09-05 补认人段、猜想折叠、逾期横幅 |
| 界面测试基建 | **早就有** | Robolectric + compose-test，26 条 |
| CI | 完成 | 单测 + 构建 + 色值漂移（漂移需 token） |
| 四栏 IA + 录音 FAB | 未做 | 现在仍是五栏 |
| 系统分享入口（A3 · P0） | **未做** | 纯客户端，零后端依赖 |
| 录前选场合 | 未做 | 场合可暂存本地随会话传 |
| 详情三 tab + 证据跳转 | 未做 | 数据 `transcript/[id]` 已够 |
| 硬件页 · 多卡 · 权益 | 未做 | 电量/存储/固件 BLE 本地可读；权益文案先固定 |
| 认人 / 预测落账 / 反馈 / 额度 | 未做 | **要 6 个 mobile 端点**，见 Phase 2 |
| 真机验证 | **一次没跑** | 所有验证都在 JVM 上 |

**结论：一个不依赖任何服务端改动的 v1 已经够「可用」。** 服务端那 6 个端点放 Phase 2。

## 推翻此前的三处

1. **「今天」不改成堆叠四段。** 现有三频道左右滑有论证（三种动作分开），拿原型覆盖它是 churn。已按频道模型补齐缺口。
2. **「记录」不新建，在 `HistoryScreen` 上长。** 它已经有四站链。把 BLE 状态条和来源分段加进去即可，不重写。
3. **硬件页第一版不显示额度数字。** 权益依赖「设备 → 权益」这张不存在的表；先显示固定文案「随卡权益：转写不限」，等 Phase 2 接真数。

## 分期

### Phase 1 · 骨架（零服务端依赖）——当前

按「不做就退货」排序，每项一个提交，单测 + 界面测试 + assembleDebug 三绿才提交。

| # | 项 | 屏 | 状态 |
| --- | --- | --- | --- |
| 1.1 | 四栏 IA：今天 / 记录 / 问 / 我的，录音改 FAB 沉浸式 | — | ✅ 09-05 |
| 1.2 | 记录 = 历史 + 台账：灵魂卡状态条、「看不到什么」（来源分段需服务端返回 capture_client，挪到 Phase 2） | S5 | ✅ 09-05 |
| 1.3 | 系统分享入口 → 同一条上传管道（半屏选场合随 1.4 补） | S6 | ✅ 09-05 |
| 1.4 | 录音：告知文案、可扣过去的提示、字幕默认收起（**选场合挪 Phase 2**：建会话 API 没有场合字段，传不上去的选择器不画） | S3 S4 | ✅ 09-05 |
| 1.5 | 详情三 tab：判断 / 承诺 / 原话（原话 = 被引用的片段 + 网页版入口；**逐句与定位到秒需 mobile 返回 segments → Phase 2**） | S8 S9 S10 | ✅ 09-05 |
| 1.6 | 硬件页：多卡列表、自命名、电量/存储/固件（**TYPE=0 按协议文档新写，无真机验证**，解不出显示「—」） | S2 S23 | ✅ 09-05 |
| 1.7 | 我的：设备入口、导出（走网页票据）、存储说明 | S16 | 进行中 |
| 1.8 | 配对三步：靠近 → 认领（画实物标按键）→ 录一段试试 | S1 | |
| 1.9 | 三态审计：逐屏过 空 / 离线 / 失败 | — | |
| 1.10 | 密度审计：逐屏过 ≤5 条 | — | |
| 1.11 | 通知四类：录音中常驻、同步、到期提醒、有新判断 | S24 | |

### Phase 2 · 服务端 6 个 mobile 端点 ——**需要你部署**

能力 web 侧都有，缺的是 Bearer 鉴权的 mobile 面：预测落账、认人、判断反馈、额度、更多入口、设备权益。
另加三处字段：`sessions` 返回 `capture_client`（记录页按来源分段）；建会话或 analyze 接受 `scene`（录前选场合 = 选方法）；`transcript/[id]` 返回 `segments`（原话 tab 逐句 + 依据定位到秒）。
我在主仓库开分支写好 + 测试绿 → **你确认后部署**（红线：未经确认不部署、不跑迁移）。
「设备 → 权益」是唯一要新建表的。

### Phase 3 · 真机与硬件 ——**需要你**

- 真机 + 灵魂卡：全链路掐表「拆盒到第一份判断」
- 各家 ROM 后台录音存活（另立 011）
- 正式签名 APK

## 必须你配合的清单

| 事 | 为什么我做不了 | 什么时候要 |
| --- | --- | --- |
| **真机 + 灵魂卡实测** | 没有硬件；Robolectric 碰不到蓝牙/麦克风/前台服务 | Phase 1 收尾 |
| **服务端部署与迁移** | CLAUDE.md 红线 | Phase 2 |
| `SHENNAO_REPO_TOKEN` | CI 的色值漂移检查要读主仓库 | 随时 |
| 灵魂卡固件支持 OTA 吗 | 协议文档没提，S23「固件更新」画不画取决于它 | 1.6 已按「不画」处理，有 OTA 再加 |
| **真机核对 TYPE=0 三个数** | 电量/容量/固件是按协议 §4 新写的，全项目从没发过这几条命令 | Phase 3 第一件事 |
| 出正式 APK 的 keystore | 在你的 `local.properties` 里，我这台机器没有 | Phase 3 |

其余一律我自己定。

## 进度日志

| 日期 | 事 |
| --- | --- |
| 09-04 | 独立成库，59 提交带历史 |
| 09-05 | 色值防线接回（快照）；导航单一真源；CI；今天页补三缺口；8 条界面测试；四栏 IA + 录音 FAB；记录页灵魂卡状态条与账本边界；系统分享入口 |
